
  <div class="stage1">


    <div id="chap1" >
        <%= audio_tag "rain.mp3", :id => "rain", :onload => "playSound()" %>
    <%= image_tag "cloud.png", :style => "width: 50%;left: -16%;position: relative;top: 102px;"%>
      <br><br><br><br><br>

<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>


    <div class="form_play">
      <% if @first_stage %>
        <%= link_to "Play", scrollz_stage_path(@scrollz, @first_stage), class: "start_button"%>
      <% end %>
      <% if current_user && @scrollz_id == current_user.id %>

      <% end %>
    </div>
  </body>
</html>


    </div>
